{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability for the main window. Grows as phases add plugins (dialog, opener, updater, fs).",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
