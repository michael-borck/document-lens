import { PlaceholderPage } from '@/components/PlaceholderPage'

export function Settings() {
  return (
    <PlaceholderPage
      title="Settings"
      subtitle="App configuration, scoring rules, data management."
    >
      <p>Backend status, app preferences, scoring rule editor, data management.</p>
    </PlaceholderPage>
  )
}
