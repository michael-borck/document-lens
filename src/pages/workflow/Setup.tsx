import { PlaceholderPage } from '@/components/PlaceholderPage'

export function Setup() {
  return (
    <PlaceholderPage
      title="Setup"
      subtitle="Assemble this project: documents, keywords, lenses, scoring rule."
    >
      <p>Four sections: Documents (subset of Library), Keywords (active list with counter-keywords), Lenses (active tag axes), Scoring rule (active rubric — default 5-level Wedding Cake Score for sustainability projects).</p>
    </PlaceholderPage>
  )
}
