import { PlaceholderPage } from '@/components/PlaceholderPage'

export function Map() {
  return (
    <PlaceholderPage
      title="Map"
      subtitle="Where in this document does each topic appear, and how do topics overlap?"
    >
      <p>One-axis mode: per-document distribution (stacked bar / donut). Two-axis mode: 2D matrix heatmap (e.g., SDG × Function). US-E-01 through US-E-05.</p>
    </PlaceholderPage>
  )
}
