import { PlaceholderPage } from '@/components/PlaceholderPage'

export function Discover() {
  return (
    <PlaceholderPage
      title="Discover"
      subtitle="What words is your corpus using that you should know about?"
    >
      <p>Two sub-tabs: Phrases (n-grams) and Synonyms (with separate positive / counter modes). Per-keyword Accept / Reject UI. US-D-01 through US-D-09.</p>
    </PlaceholderPage>
  )
}
